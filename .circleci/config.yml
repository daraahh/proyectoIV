version: 2
jobs:
    build:
        docker:
            - image: circleci/ruby:2.6.4
        steps:
            - checkout
            - run:
                name: Dependencias
                command: bundle install
            - run:
                name: Tests
                command: rake
